NEXT_PUBLIC_API_BASE_URL=http://localhost:8001/api
NEXT_PUBLIC_PROTOTYPE_PREVIEW_ORIGIN=http://localhost:3000

# Set to true to keep the frontend on local mock data instead of the backend API/SSE.
NEXT_PUBLIC_TEST_MODE=false
